import { defineConfig } from 'windicss/helpers'

export default defineConfig({
  shortcuts: {
    btn: 'py-2 px-4 rounded-md shadow-sm font-semibold opacity-90 hover:opacity-100 hover:shadow-md'
  }
})
